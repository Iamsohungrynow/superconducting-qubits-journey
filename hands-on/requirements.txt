# Dependencies for the hands-on QuTiP simulation labs.
# Verified with qutip 5.3 / numpy 2.x on Python 3.12. The labs use QuTiP's
# core API, so other 5.x versions work fine too. (QuTiP 4.7 also runs labs
# 01-04, but it needs Python <= 3.11 and numpy < 2.)
#   pip install -r hands-on/requirements.txt
qutip>=5.0
numpy>=1.24
scipy>=1.10
matplotlib>=3.5
